import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// this is where React renders the app to the DOM. its like entry point to the DOM
ReactDOM.render(<App />, document.getElementById('root'));