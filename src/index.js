import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// this is where React renders the App component to the DOM. it is React's entry point to the DOM
ReactDOM.render(<App />, document.getElementById('root'));